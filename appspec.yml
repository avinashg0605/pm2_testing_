version: 0.0
os: linux

files:
  - source: /
    destination: /opt/pm2_test

hooks:
  ApplicationStop:
    - location: test-scripts/stop_server.sh
      timeout: 300
      runas: root

  ApplicationStart:
    - location: test-scripts/start_server.sh
      timeout: 300
      runas: root